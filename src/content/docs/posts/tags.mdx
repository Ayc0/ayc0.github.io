---
title: Full list of posts per tags
template: splash
---

import LinkToPost from "@components/LinkToPost.astro";
import { getPostsByTags } from "@components/get-posts";

<a href="" data-view-all>
  ← View all
</a>
{Object.entries(await getPostsByTags()).map(([tag, posts]) => {
  return (
    <>
      <h2 id={tag}>
        <a href={`#${tag}`}>#{tag}</a>
      </h2>
      <ul>
        {posts.map((post) => (
          <li>
            <a href={`/${post.slug}`}>{post.data.title}</a> |{" "}
            {post.data.tags.map((tag) => `#${tag}`).join(" ")}
          </li>
        ))}
      </ul>
    </>
  );
})}

<style>
  {`
        body:not(:has(:target)) a[data-view-all] {
            display: none;
        }

        body:has(:target) h2[id]:not(:target) {
            display: none;
        }
        body:has(:target) h2[id]:not(:target) + ul {
            display: none;
        }
    `}
</style>
